package lpbcast;

/**
 * Class that is used to define a generic message.
 * 
 * Each message sent inside the system will extend this class.
 * A message has to define a sender.
 */
public class Message {
	int sender;
}
